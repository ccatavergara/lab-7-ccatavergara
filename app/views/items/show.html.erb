<div class='container'>
  <p style="color: green"><%= notice %></p>

  <%= render @item %>

  <div>
    <%= link_to "Edit", edit_restaurant_item_path(@item.restaurant_id, @item), class: 'btn btn-outline-warning btn-sm' %> |

    <br><br>
    <%= link_to "Back to items", restaurant_path(@item.restaurant_id), class: 'btn btn-primary btn-sm' %>

    
  </div>
</div>